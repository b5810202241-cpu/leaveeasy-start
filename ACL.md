# ACL — สิทธิ์การใช้งานตามบทบาท (LeaveEasy)

ตารางสิทธิ์ของผู้ใช้ 3 บทบาทในระบบ LeaveEasy อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 (ผู้ใช้ 3 บทบาท) และหัวข้อ 6 (สถานะและการเปลี่ยนสถานะ)

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **พนักงาน** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | อนุมัติหรือไม่อนุมัติใบลา แม้จะเป็นใบของตัวเองก็ตาม · ดูใบลาของคนอื่น · ลบใบลาของคนอื่น · แก้ไข/เพิ่ม/ลบประเภทการลา · กำหนดผู้อนุมัติให้ใบลา |
| ✅ **หัวหน้า** (`manager`) | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของใครก็ตาม (ไม่มีสิทธิ์ลบใบลาเลย ไม่ว่าของตัวเองหรือคนอื่น) · แก้ไข/เพิ่ม/ลบประเภทการลา · กำหนดผู้อนุมัติให้ใบลา |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่หัวหน้าทำได้ (ดูทุกใบ · เปลี่ยนสถานะ · เขียนความเห็น) · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบประเภทการลา · เห็นแดชบอร์ดสรุปทั้งระบบ *(หน้าโครงจาก prototype — ต่อข้อมูลจริงใน Module 3)* | ลบใบลาของใครก็ตาม (สิทธิ์ลบที่ระบุไว้มีแค่ "ลบประเภทการลา" เท่านั้น ไม่ใช่ลบใบลา) |

## กฎร่วมทุกบทบาท (ไม่ขึ้นกับสิทธิ์)

- ใบลาใหม่ทุกใบเริ่มที่สถานะ `รอพิจารณา` เสมอ ไม่มีใครเลือกสถานะเริ่มต้นเองได้
- เปลี่ยนสถานะได้ทางเดียว: `รอพิจารณา` → `อนุมัติ` หรือ `ไม่อนุมัติ` เท่านั้น ห้ามย้อนกลับ
- ใบที่สถานะ `อนุมัติ`/`ไม่อนุมัติ` แล้ว ลบไม่ได้ไม่ว่าบทบาทใด (ลบได้เฉพาะใบสถานะ `รอพิจารณา`)
- เปลี่ยนเป็น `ไม่อนุมัติ` ต้องมีความเห็นในใบนั้นอย่างน้อย 1 รายการก่อนเสมอ

## สถานะการบังคับใช้จริง

ตอนนี้ (สัปดาห์ที่ 7) `firestore.rules` บังคับแค่กฎขั้นต่ำ **"ต้องล็อกอินก่อน ถึงอ่าน/เขียนได้"** — ยังไม่แยกสิทธิ์ตามบทบาทในตารางนี้จริงในฐานข้อมูล การแยกสิทธิ์รายบทบาท (เช่น พนักงานเห็นแค่ใบของตัวเอง, พนักงานอนุมัติใบลาตัวเองไม่ได้) จะเขียนเป็น Security Rules จริงในสัปดาห์ที่ 8 ตามแผนในหัวข้อ 8 ของ `leaveeasy-spec.md` — ตารางนี้คือ spec ที่กฎชุดนั้นต้องเขียนให้ตรง
